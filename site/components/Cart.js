import CartProducts from "../components/CartProducts";
import CartSize from "../components/CartSize";

const Cart = () => (
  <>
    <h2>Your cart</h2>
    <CartProducts />
    <CartSize />
  </>
);

export default Cart;
